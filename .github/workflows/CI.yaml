name: CI

on:
  push:
  brnaches: [ main ]


jobs:
 test: 
  name: test on Node.jobs
  runs-on: unbuntu-latest

  steps:
    name: Checkout code
    uses: actions/checkout@v5
    name: Set up Node.js
    uses: actions/setup-node@v4
    with:
    node-version: '16'
    name: Install dependencies
    run: npm install
    name: Run tests
    run: npm test
   

  
